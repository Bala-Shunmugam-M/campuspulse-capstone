# CampusPulse - Python dependencies
# Install with:  pip install -r requirements.txt

psycopg2-binary==2.9.10   # PostgreSQL driver (binary wheel - no C build needed)
python-dotenv==1.0.1      # Loads .env into the environment
Faker==33.1.0             # Realistic synthetic names, emails, text
tabulate==0.9.0           # Pretty console tables for verification + analytics
